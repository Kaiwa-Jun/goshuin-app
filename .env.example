# Supabase
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# RevenueCat の公開 SDK キー（iOS・appl_ で始まる。秘密ではない）。本番・preview は EAS の環境変数に入れる
# Test Store のキー（test_ で始まる）は .env.local だけに書く。git と EAS には入れない（本番に入れて提出してはいけない）
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
