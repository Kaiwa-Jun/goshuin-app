# 2026-10 の再開メモ（2026-09-23 時点の状態）

**止まっている理由**: EAS の無料プランの iOS ビルド回数を 9 月分で使い切った（リセットは **2026-10-01**）。
コードはすべて develop にマージ済み。残りは「ビルド → 実機確認 → 審査」だけ。

## 再開したら最初にやること（この順）

1. **ビルド 20 を作って TestFlight に出す**（develop の HEAD＝PR #242 以降）
   ```
   git fetch && git checkout --detach origin/develop
   npx eas-cli build --platform ios --profile production --non-interactive --no-wait
   # 終わったら
   npx eas-cli submit --platform ios --id <ビルドID> --non-interactive
   ```
   ⚠ TestFlight の内部グループ「Internal」は **新しいビルドを自動で受け取らない**（hasAccessToAllBuilds=false）。
   Apple の処理（5〜10分・`node scripts/asc-review.mjs status` で見られる）が終わったら、
   **`node scripts/asc-review.mjs testflight`** で最新ビルドを Internal グループに追加する（2026-09-23 はビルド 18・19 をこれと同じ API で追加）
2. **実機で3点を確認**（PR #242 / #246）
   - アプリを**削除して入れ直して**起動 → オンボーディング1枚目が透けて見えない（起動画面 → オンボーディング が1回だけ）
   - テスト用アカウントで**最初の1枚**を記録 → 完了画面で写真がカードからはみ出さない（終わったらテスト用アカウントは削除）
   - 本アカウントで**あゆみ**を開く → 一番上に「もう少し」（仙台六芒星巡り あと1社・仙台のまわり）。帯・丸・輪の見た目、エリアのシート、各行を押した先
3. 問題なければ **1.2.0 を審査に出す**か、**先に S4a（PR #239）を入れて出す**かを決める
   - 契約書どおりなら: 1.2.0（S3 のみ）を出す → 公開 → S4a をマージ → 1.3.0
   - What's New（store-metadata.md）は 1.2.0 用にまだ書いていない。利用者から見える変化は「あゆみの「もう少し」」「完了画面の見た目」「起動時のちらつき」「削除後に開けない不具合」の修正くらい

## 1.2.0（ビルド 18/19 → 20）に入っているもの

| PR   | 内容                                                                           | 実機確認                                            |
| ---- | ------------------------------------------------------------------------------ | --------------------------------------------------- |
| #237 | **#227 S3**: 写真を Supabase と R2 の両方に書き・両方から消す                  | ✅ 9/23 ビルド 18/19 で AC-5/9/10/11 すべて確認済み |
| #241 | 御朱印帳で詳細から削除すると、残りの御朱印が開かなくなる（**1.1.0 から存在**） | ✅ ビルド 19 で開けることを確認                     |
| #242 | 初回起動のちらつき / 初回投稿の完了画面のはみ出し                              | ⏳ ビルド 20 で確認                                 |
| #246 | あゆみの一番上に「もう少し」（#245）                                           | ⏳ ビルド 20 で確認                                 |
| #232 | アプリ内プライバシーポリシーに Cloudflare を追記                               | —（公開 URL 版は main で反映済み）                  |

## Cloudflare 移行（#227 Phase 1）の状況

契約書: `docs/issues/issue-227-r2-image-migration.md`（S1〜S3 の本番記録あり）

| スライス                | 状態                                                                                                                                                                                           |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1 土台                 | ✅ 完了。`goshuinsanpo.com`（Cloudflare Registrar・$10.46/年・自動更新）、R2 `goshuin-images`、`img.goshuinsanpo.com`、変換有効化、CORS、Edge Function `sign-stamp-upload`（本番デプロイ済み） |
| S2 プライバシーポリシー | ✅ 公開 URL に反映済み（main / PR #236）                                                                                                                                                       |
| S3 二重書き込み         | ✅ サーバー側は本番反映・確認済み（delete-account 再デプロイ済み、既存 62 枚コピー済み）。アプリ側は 1.2.0 で配信待ち                                                                          |
| S4a 読み取り切替        | 🟡 **PR #239 はマージ保留**。Evaluator PASS。1.2.0 が出てからマージする約束。マージ前に develop へ rebase が要るかもしれない                                                                   |
| S4b 書き込み切替        | 未着手（S4a を本番で1リリース以上運用してから）                                                                                                                                                |
| S5 後片付け             | 未着手（Supabase は読み取り専用にするまで。削除しない）                                                                                                                                        |

覚えておくこと:

- `format=webp` も `Accept` を見る（無いと JPEG が返る）。S4a の実機確認では返った Content-Type を記録する
- 削除直後は CDN のキャッシュから数秒〜しばらく古い画像が返ることがある（実体は消えている）。気になるなら削除時の purge を別 Issue に
- 1.1.0 以前のアプリは Supabase にしか書かない。その分は R2 に無いので、一覧・全画面は Supabase の原本にフォールバックする（S4a の設計どおり）。コピーを定期実行する仕組みは作らない（D-4）
- R2 のトークンは2つ: `goshuin-app-edge-function`（Supabase secrets に登録済み・無期限）と コピー用（TTL 1日・失効済みのはず）。**Secret は作成時にしか見えない**
- 本番 DB の書き換え・関数デプロイは Claude の auto mode では止まる。オーナーに `!` で実行してもらう。R2 の鍵を使うコマンドは `!` を付けずにオーナーのターミナルで

## オーナーへの確認事項（未対応）

- シェル履歴（`~/.zsh_history`）に R2 の鍵を直接書いたコマンドが残っていないか。無期限の Edge Function 用の鍵なら削除する
- EAS のビルド回数: 1回の確認でビルドを何本も使う（今回 18/19/20）。10 月も足りなくなるなら有料プラン（Starter $19/月）かローカルビルドを検討

## 9/23 に閉じたもの

- #225 他人のフォルダに画像を書けるポリシー → 本番適用・実機確認済み
- #226 退会時に縮小版が消え残る → 本番デプロイ・実機確認済み
- 1.1.0 → 9/23 に App Store で公開（READY_FOR_SALE）
